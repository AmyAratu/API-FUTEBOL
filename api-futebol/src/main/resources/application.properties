# URL do banco de dados MySQL no Docker
spring.datasource.url=jdbc:mysql://localhost:3306/api.futebol
# Nome de usuário e senha do banco de dados
spring.datasource.username=usuario_api
spring.datasource.password=senha_segura

# Driver JDBC para MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Níveis de log do Hibernate
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE